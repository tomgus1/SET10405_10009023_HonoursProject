[package]
name = "notes-app"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
unicode-segmentation = "1"

[dev-dependencies]
tempfile = "3"
