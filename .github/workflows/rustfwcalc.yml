name: Build rustfwcalc

on:
  push:
    branches: [main]
    paths:
      - 'calc/rustfwcalc/**'
      - '.github/workflows/rustfwcalc.yml'
  pull_request:
    paths:
      - 'calc/rustfwcalc/**'
      - '.github/workflows/rustfwcalc.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: calc/rustfwcalc

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            bin_suffix: ''
          - os: windows-latest
            bin_suffix: '.exe'
          - os: macos-latest
            bin_suffix: ''
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: calc/rustfwcalc

      - name: Install Linux GUI dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc g++ libasound2-dev libfontconfig-dev libgit2-dev libglib2.0-dev \
            libssl-dev libva-dev libvulkan1 libwayland-dev libx11-xcb-dev \
            libxkbcommon-x11-dev libzstd-dev make cmake clang lld llvm jq \
            gettext-base elfutils libsqlite3-dev build-essential pkg-config \
            pipewire xdg-desktop-portal libstdc++-12-dev

      - run: cargo test --release

      - run: cargo build --release

      - name: Package Linux AppImage
        if: runner.os == 'Linux'
        run: ../../.github/scripts/make-appimage.sh rustfwcalc target/release/rustcalc

      - name: Upload Linux AppImage
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: rustfwcalc-linux
          path: calc/rustfwcalc/*.AppImage

      - name: Ad-hoc codesign macOS binary
        if: runner.os == 'macOS'
        run: codesign --force --sign - target/release/rustcalc

      - name: Upload build
        if: runner.os != 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: rustfwcalc-${{ matrix.os }}
          path: calc/rustfwcalc/target/release/rustcalc${{ matrix.bin_suffix }}
